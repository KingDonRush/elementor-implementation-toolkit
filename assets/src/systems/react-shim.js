const React = window.wp.element;

export default React;
export const {
	Component,
	createContext,
	forwardRef,
	memo,
	useCallback,
	useContext,
	useDebugValue,
	useEffect,
	useLayoutEffect,
	useMemo,
	useRef,
	useState,
	useSyncExternalStore,
} = React;
